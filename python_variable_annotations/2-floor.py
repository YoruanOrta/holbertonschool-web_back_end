#!/usr/bin/env python3
"""This module contain a function `floor` that returns the floor of a float."""
import math


def floor(n: float) -> int:
    """Returns the floor of the float n."""
    return math.floor(n)
